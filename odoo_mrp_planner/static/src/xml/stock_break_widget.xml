<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

    <t t-name="odoo_mrp_planner.StockBreakWidget">
        <div class="o_stock_break_widget" t-att-class="state.loading ? 'opacity-50 pe-none' : ''">

            <!-- Error de backend -->
            <div t-if="state.loadError" class="alert alert-danger py-2 mb-2" role="alert">
                <i class="fa fa-exclamation-triangle me-2"/>
                Error al cargar quiebres de stock: <strong><t t-esc="state.loadError"/></strong>
            </div>

            <!-- Sin ubicación configurada -->
            <div t-if="state.error === 'no_location'" class="alert alert-warning d-flex align-items-center gap-2 py-2">
                <i class="fa fa-map-marker fa-lg"/>
                <span>No hay ubicación de stock configurada para este widget.</span>
                <button class="btn btn-sm btn-warning ms-auto" t-on-click="openConfig">
                    Configurar →
                </button>
            </div>

            <t t-else="">

                <!-- Ubicación(es) activa(s) -->
                <div class="text-muted small mb-2">
                    <i class="fa fa-map-marker me-1"/>
                    <t t-esc="state.locationName || '—'"/>
                </div>

                <!-- KPI cards -->
                <div class="row g-2 mb-3">
                    <div class="col-6 col-md-3">
                        <div class="card o_planner_card h-100" style="cursor:pointer"
                             t-att-title="stockKpiTooltip('total')"
                             t-on-click="() => this.setFilter('all')">
                            <div class="card-body text-center py-3">
                                <i class="fa fa-cubes fa-lg text-secondary mb-2 d-block"/>
                                <div class="o_planner_number"><t t-esc="liveKpis.total"/></div>
                                <div class="o_planner_label">Vendibles</div>
                            </div>
                            <div class="card-footer p-0">
                                <button class="btn btn-sm btn-link w-100 py-1" t-on-click.stop="() => this.openKpiProducts('all')">Ver →</button>
                            </div>
                        </div>
                    </div>
                    <div class="col-6 col-md-3">
                        <div class="card o_planner_card o_planner_card_danger h-100" style="cursor:pointer"
                             t-att-title="stockKpiTooltip('broken')"
                             t-on-click="() => this.setFilter('broken')">
                            <div class="card-body text-center py-3">
                                <i class="fa fa-exclamation-triangle fa-lg text-danger mb-2 d-block"/>
                                <div class="o_planner_number"><t t-esc="liveKpis.broken"/></div>
                                <div class="o_planner_label">Quiebres</div>
                            </div>
                            <div class="card-footer p-0">
                                <button class="btn btn-sm btn-link w-100 py-1" t-on-click.stop="() => this.openKpiProducts('broken')">Ver →</button>
                            </div>
                        </div>
                    </div>
                    <div class="col-6 col-md-3">
                        <div class="card o_planner_card o_planner_card_success h-100" style="cursor:pointer"
                             t-att-title="stockKpiTooltip('ok')"
                             t-on-click="() => this.setFilter('ok')">
                            <div class="card-body text-center py-3">
                                <i class="fa fa-check-circle fa-lg text-success mb-2 d-block"/>
                                <div class="o_planner_number"><t t-esc="liveKpis.ok"/></div>
                                <div class="o_planner_label">Stock OK</div>
                            </div>
                            <div class="card-footer p-0">
                                <button class="btn btn-sm btn-link w-100 py-1" t-on-click.stop="() => this.openKpiProducts('ok')">Ver →</button>
                            </div>
                        </div>
                    </div>
                    <div class="col-6 col-md-3">
                        <div class="card o_planner_card h-100" style="cursor:pointer"
                             t-att-title="stockKpiTooltip('no_min')"
                             t-on-click="() => this.setFilter('no_min')">
                            <div class="card-body text-center py-3">
                                <i class="fa fa-minus-circle fa-lg text-muted mb-2 d-block"/>
                                <div class="o_planner_number"><t t-esc="liveKpis.no_min"/></div>
                                <div class="o_planner_label">Sin mínimo</div>
                            </div>
                            <div class="card-footer p-0">
                                <button class="btn btn-sm btn-link w-100 py-1" t-on-click.stop="() => this.openKpiProducts('no_min')">Ver →</button>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Buscador + filtro + selector de depósito -->
                <div class="d-flex align-items-center gap-2 mb-3 flex-wrap">
                    <!-- Barra de búsqueda estilo Odoo -->
                    <PlannerSearchBar
                        widgetKey="'stock_break'"
                        placeholder="'Buscar producto o código…'"
                        t-props="{
                            search:        state.search,
                            activeFilter:  state.filterType !== 'all' ? state.filterType : null,
                            activeGroupBy: state.groupBy,
                            filterDefs: [
                                { key: 'broken', label: 'Quiebres',  icon: 'fa-exclamation-triangle' },
                                { key: 'ok',     label: 'OK',        icon: 'fa-check-circle' },
                                { key: 'no_min', label: 'Sin mínimo',icon: 'fa-minus-circle' }
                            ],
                            groupByDefs:    stockGroupByDefs,
                            onSearch:       (t) => this.setSearch(t),
                            onFilterChange: (k) => this.setFilterDirect(k),
                            onGroupByChange:(k) => this.onGroupByChange(k),
                            numericFields:     numColOptions,
                            numFilters:        state.numFilters,
                            onNumFilterAdd:    (c) => this.addNumFilter(c),
                            onNumFilterRemove: (i) => this.removeNumFilter(i)
                        }"/>
                    <div t-if="state.locations.length" class="d-flex align-items-center gap-1" t-on-click.stop="">
                        <label class="form-label mb-0 small text-muted">Depósito</label>
                        <div class="position-relative">
                            <button class="btn btn-sm btn-outline-secondary d-flex align-items-center gap-1"
                                    style="min-width:160px;"
                                    t-on-click="toggleLocDropdown">
                                <span class="text-truncate" style="max-width:130px;"><t t-esc="selectedLocLabel"/></span>
                                <i t-att-class="'fa ms-auto ' + (state.locDropdownOpen ? 'fa-chevron-up' : 'fa-chevron-down')"/>
                            </button>
                            <div t-if="state.locDropdownOpen"
                                 class="dropdown-menu show shadow p-2"
                                 style="min-width:220px; position:absolute; z-index:1050; top:100%; left:0; margin-top:2px;">
                                <input type="text" class="form-control form-control-sm mb-2"
                                       placeholder="Buscar depósito…"
                                       t-model="state.locSearch"
                                       autocomplete="off"/>
                                <div class="form-check mb-1">
                                    <input type="checkbox" class="form-check-input" id="loc_filter_all"
                                           t-att-checked="!state.locationIds.length"
                                           t-on-change="clearLocFilter"/>
                                    <label class="form-check-label fw-semibold small" for="loc_filter_all">Todas</label>
                                </div>
                                <div class="dropdown-divider my-1"/>
                                <t t-foreach="filteredLocations" t-as="loc" t-key="loc.id">
                                    <div class="form-check mb-1">
                                        <input type="checkbox" class="form-check-input"
                                               t-att-id="'loc_filter_' + loc.id"
                                               t-att-data-loc-id="loc.id"
                                               t-att-checked="state.locationIds.includes(loc.id)"
                                               t-on-change="toggleLocation"/>
                                        <label class="form-check-label small" t-att-for="'loc_filter_' + loc.id">
                                            <t t-esc="loc.name"/>
                                        </label>
                                    </div>
                                </t>
                            </div>
                        </div>
                    </div>
                    <div class="ms-auto">
                        <button class="btn btn-sm btn-outline-success d-flex align-items-center gap-1"
                                t-on-click="downloadExport">
                            <i class="fa fa-download"/>
                            <span>Exportar</span>
                        </button>
                    </div>
                </div>

                <!-- Tabs de agrupamiento -->
                <t t-if="state.groupBy and allGroupsForTabs">
                    <ul class="nav nav-tabs mb-0" style="flex-wrap:nowrap; overflow-x:auto;">
                        <t t-foreach="allGroupsForTabs" t-as="g" t-key="'tab_' + g.key">
                            <li class="nav-item">
                                <button t-att-class="'nav-link px-3 py-1 ' + (state.selectedGroup === g.key ? 'active fw-semibold' : 'text-muted')"
                                        style="font-size:12px; white-space:nowrap;"
                                        t-att-title="'Ver solo los ' + g.count + ' artículo(s) del grupo: ' + g.label"
                                        t-on-click="() => this.setGroup(g.key)">
                                    <t t-if="state.groupBy === 'sale_category'">
                                        <span t-att-class="saleCatBadge(g.key)"
                                              style="font-size:10px; padding:1px 6px; margin-right:4px;">
                                            <t t-esc="g.label"/>
                                        </span>
                                    </t>
                                    <t t-else=""><t t-esc="g.label"/></t>
                                    <span class="text-muted ms-1" style="font-size:11px;">(<t t-esc="g.count"/>)</span>
                                </button>
                            </li>
                        </t>
                    </ul>
                </t>

                <!-- Selección activa: KPIs y totales se recalculan sobre ella -->
                <div t-if="selectedRows.length" class="d-flex align-items-center gap-2 mb-2">
                    <span class="badge text-bg-primary">
                        <t t-esc="selectedRows.length"/> seleccionado(s)
                    </span>
                    <span class="text-muted small">Los KPIs y totales reflejan la selección.</span>
                    <button class="btn btn-sm btn-link p-0" t-on-click="clearSelection">Limpiar</button>
                </div>

                <!-- Estado vacío (solo cuando no hay agrupamiento activo) -->
                <div t-if="!state.groupBy and state.products.length === 0"
                     class="text-center text-muted py-4 border rounded">
                    <i class="fa fa-check-circle fa-2x text-success mb-2 d-block"/>
                    No hay productos en esta categoría.
                </div>

                <t t-if="(state.groupBy and allGroupsForTabs and allGroupsForTabs.length) or state.products.length">
                    <div class="border rounded" style="max-height:72vh; overflow:auto;">
                        <table class="table table-sm table-hover small mb-0" style="table-layout:fixed;">
                            <colgroup>
                                <col style="width:28px;"/>
                                <col t-foreach="visibleStockCols" t-as="col" t-key="col.key"
                                     t-att-style="colsStock.colGroupStyle(col.key)"/>
                            </colgroup>
                            <thead class="table-light" style="position:sticky; top:0; z-index:3;">
                                <tr class="text-muted">
                                    <th class="text-center align-middle" style="user-select:none;">
                                        <input type="checkbox" class="form-check-input"
                                               title="Seleccionar todos los artículos de la página"
                                               t-att-checked="allSelected"
                                               t-on-change="toggleSelectAll"/>
                                    </th>
                                    <th t-foreach="visibleStockCols" t-as="col" t-key="col.key"
                                        t-att-class="(col.align ? 'text-' + col.align + ' ' : '') + (colsStock.state.dragOver === col.key ? 'o_col_drag_over ' : '')"
                                        style="position:relative; user-select:none;"
                                        t-att-title="colHeaderTitle(col)"
                                        t-att-data-col-key="col.key"
                                        t-att-data-sort-key="col.sortKey || ''"
                                        t-att-draggable="col.fixed ? 'false' : 'true'"
                                        t-on-click="onHeaderClick"
                                        t-on-dragstart="colsStock.onDragStart"
                                        t-on-dragover="colsStock.onDragOver"
                                        t-on-drop="colsStock.onDrop"
                                        t-on-dragend="colsStock.onDragEnd">
                                        <t t-esc="col.label"/>
                                        <i t-if="col.sortKey" t-att-class="sortIcon(col.sortKey)"/>
                                        <div t-if="!col.noResize" class="o_col_resize_handle"
                                             t-att-data-col-key="col.key"
                                             t-on-mousedown.stop="colsStock.onResizeStart"
                                             t-on-click.stop="() => {}"/>
                                    </th>
                                </tr>
                            </thead>
                            <tbody>
                                <t t-foreach="state.products" t-as="prod" t-key="prod.id">
                                    <!-- Fila principal -->
                                    <tr t-att-class="prod.is_broken ? 'table-danger' : ''"
                                        style="cursor:pointer"
                                        t-att-data-product-id="prod.id"
                                        t-on-click="openProductFromRow">
                                        <td class="text-center align-middle" t-on-click.stop="">
                                            <input type="checkbox" class="form-check-input"
                                                   t-att-checked="state.selected[prod.id]"
                                                   title="Seleccionar: los KPIs y totales se recalculan sobre la selección"
                                                   t-on-change="() => this.toggleSelect(prod)"/>
                                        </td>
                                        <td t-foreach="visibleStockCols" t-as="col" t-key="col.key"
                                            t-att-class="(col.align ? 'text-' + col.align + ' ' : '') + (col.key === '_expand' ? 'text-center align-middle p-0' : '')">
                                            <t t-if="col.key === '_expand'">
                                                <button t-if="prod.mo_count"
                                                        class="btn btn-sm btn-link p-0 px-1 text-muted"
                                                        t-att-title="state.expandedProducts[prod.id] ? 'Cerrar OFs' : 'Ver OFs activas'"
                                                        t-on-click.stop="(ev) => this.toggleAccordion(prod, ev)">
                                                    <i t-att-class="'fa fa-fw ' + (state.expandedProducts[prod.id] ? 'fa-chevron-down' : 'fa-chevron-right')"/>
                                                </button>
                                            </t>
                                            <t t-elif="col.key === 'name'">
                                                <span class="fw-semibold text-truncate d-block" t-esc="prod.name"/>
                                            </t>
                                            <t t-elif="col.key === 'product_types'">
                                                <span class="text-truncate text-muted small d-block"
                                                      t-att-title="prod.product_types || ''"
                                                      t-esc="prod.product_types || '—'"/>
                                            </t>
                                            <t t-elif="col.key === 'sale_category'">
                                                <span t-if="prod.sale_category"
                                                      t-att-class="saleCatBadge(prod.sale_category)"
                                                      t-att-title="saleCatTooltip()"
                                                      t-esc="prod.sale_category"/>
                                                <span t-else="" class="text-muted">—</span>
                                            </t>
                                            <t t-elif="col.key === 'categ_name'">
                                                <span class="text-truncate small d-block"
                                                      t-att-title="prod.categ_name || ''"
                                                      t-esc="prod.categ_name || '—'"/>
                                            </t>
                                            <t t-elif="col.key === 'qty'">
                                                <t t-esc="fmt(prod.qty)"/>
                                            </t>
                                            <t t-elif="col.key === 'min_qty'">
                                                <t t-if="prod.has_min" t-esc="fmt(prod.min_qty)"/>
                                                <span t-else="" class="text-muted">—</span>
                                            </t>
                                            <t t-elif="col.key === 'qty_forecast'">
                                                <t t-if="prod.qty_forecast !== null and prod.qty_forecast !== undefined"
                                                   t-esc="fmt(prod.qty_forecast)"/>
                                                <span t-else="" class="text-muted">—</span>
                                            </t>
                                            <t t-elif="col.key === 'rotation'">
                                                <span t-att-class="rotClass(prod)"
                                                      t-att-title="rotTooltipStock(prod)">
                                                    <t t-esc="fmtRotation(prod)"/>
                                                    <i t-if="prod.rotation_days !== null
                                                             and prod.rotation_days !== undefined
                                                             and state.rotation_critical_days !== null
                                                             and prod.rotation_days > state.rotation_critical_days"
                                                       class="fa fa-exclamation-triangle ms-1"
                                                       style="font-size:10px;"
                                                       t-att-title="`Rotación lenta: supera el umbral crítico de ${state.rotation_critical_days} días`"/>
                                                </span>
                                            </t>
                                            <t t-elif="col.key === 'bom_lead'">
                                                <span t-if="prod.bom_lead_days !== null and prod.bom_lead_days !== undefined"
                                                      t-att-title="bomLeadTooltip(prod)">
                                                    <t t-esc="prod.bom_lead_days + ' d'"/>
                                                </span>
                                                <span t-else="" class="text-muted" title="Sin BoM">—</span>
                                            </t>
                                            <t t-elif="col.key === 'status'">
                                                <span t-if="prod.is_broken"
                                                      class="badge text-bg-danger"
                                                      t-att-title="statusTooltip(prod)">
                                                    <t t-esc="breakLabel(prod)"/>
                                                </span>
                                                <span t-elif="prod.has_min"
                                                      class="badge text-bg-success"
                                                      t-att-title="statusTooltip(prod)">
                                                    OK
                                                </span>
                                                <span t-else=""
                                                      class="text-muted fst-italic small"
                                                      title="Sin punto de reorden con ruta Fabricación">
                                                    Sin mínimo
                                                </span>
                                            </t>
                                        </td>
                                    </tr>
                                    <!-- Acordeón: OFs activas -->
                                    <tr t-if="prod.mo_count and state.expandedProducts[prod.id]">
                                        <td t-att-colspan="visibleStockCols.length + 1" class="p-0" style="background:#f8f9fa; border-top:none;">
                                            <div class="px-3 py-2">

                                                <div t-if="state.mosLoading[prod.id]"
                                                     class="text-center text-muted py-2 small">
                                                    <i class="fa fa-spinner fa-spin me-1"/> Cargando OFs…
                                                </div>

                                                <div t-elif="!state.mosByProduct[prod.id] or !state.mosByProduct[prod.id].length"
                                                     class="text-muted small py-1">
                                                    <i class="fa fa-info-circle me-1"/>
                                                    No hay órdenes de fabricación activas para este producto.
                                                </div>

                                                <table t-else=""
                                                       class="table table-sm table-hover mb-0"
                                                       style="font-size:11px; background:white;">
                                                    <thead>
                                                        <tr class="text-muted">
                                                            <th style="padding:3px 8px;">Referencia</th>
                                                            <th style="padding:3px 8px;">Estado</th>
                                                            <th class="text-end" style="padding:3px 8px;">Planificado</th>
                                                            <th class="text-end" style="padding:3px 8px;">Producido</th>
                                                            <th style="padding:3px 8px;">Fin planificado</th>
                                                        </tr>
                                                    </thead>
                                                    <tbody>
                                                        <t t-foreach="state.mosByProduct[prod.id]" t-as="mo" t-key="mo.id">
                                                            <tr style="cursor:pointer;"
                                                                t-on-click="() => this.openMo(mo.id)"
                                                                title="Abrir orden de fabricación">
                                                                <td style="padding:3px 8px;" class="text-primary fw-semibold">
                                                                    <i class="fa fa-external-link me-1" style="font-size:10px;"/>
                                                                    <t t-esc="mo.name"/>
                                                                </td>
                                                                <td style="padding:3px 8px;">
                                                                    <span t-att-class="moStateBadge(mo.state)"
                                                                          style="font-size:10px; padding:2px 5px;">
                                                                        <t t-esc="mo.state_label"/>
                                                                    </span>
                                                                </td>
                                                                <td class="text-end" style="padding:3px 8px;">
                                                                    <t t-esc="fmt(mo.product_qty)"/>
                                                                    <span class="text-muted ms-1"><t t-esc="mo.uom"/></span>
                                                                </td>
                                                                <td class="text-end" style="padding:3px 8px;">
                                                                    <t t-esc="fmt(mo.qty_produced)"/>
                                                                </td>
                                                                <td style="padding:3px 8px;" class="text-muted">
                                                                    <t t-esc="mo.date_finished"/>
                                                                </td>
                                                            </tr>
                                                        </t>
                                                    </tbody>
                                                </table>
                                            </div>
                                        </td>
                                    </tr>
                                </t>
                            </tbody>
                            <!-- Totales del dataset filtrado (todas las páginas). Sumas como
                                 las listas de Odoo (sin distinguir UdM); promedios en plazo
                                 de fabricación y rotación. -->
                            <tfoot t-if="state.products.length" class="table-light fw-semibold"
                                   style="border-top:2px solid #dee2e6;">
                                <tr>
                                    <td/>
                                    <td t-foreach="visibleStockCols" t-as="col" t-key="col.key"
                                        t-att-class="col.align ? 'text-' + col.align : ''">
                                        <t t-if="col.key === 'name'">
                                            Totales (<t t-esc="stockTotals.count"/>)
                                        </t>
                                        <t t-elif="col.key === 'qty'">
                                            <span title="Suma del stock actual de las filas filtradas (mezcla UdM si los artículos difieren)."
                                                  t-esc="fmt(stockTotals.qty)"/>
                                        </t>
                                        <t t-elif="col.key === 'min_qty'">
                                            <span title="Suma de los mínimos de reorden de las filas filtradas."
                                                  t-esc="fmt(stockTotals.min_qty)"/>
                                        </t>
                                        <t t-elif="col.key === 'qty_forecast'">
                                            <span title="Suma del pronóstico de las filas filtradas."
                                                  t-esc="fmt(stockTotals.qty_forecast)"/>
                                        </t>
                                        <t t-elif="col.key === 'bom_lead'">
                                            <span t-if="stockTotals.bom_lead_avg !== null"
                                                  title="Promedio del plazo de fabricación de las filas con BoM."
                                                  t-esc="'⌀ ' + stockTotals.bom_lead_avg + ' d'"/>
                                        </t>
                                        <t t-elif="col.key === 'rotation'">
                                            <span t-if="stockTotals.rotation_avg !== null"
                                                  title="Promedio de rotación de las filas con dato."
                                                  t-esc="'⌀ ' + fmt(stockTotals.rotation_avg) + (state.rotation_unit === 'months' ? ' m' : ' d')"/>
                                        </t>
                                    </td>
                                </tr>
                            </tfoot>
                        </table>
                    </div>

                    <!-- Paginación -->
                    <div t-if="totalPages > 1"
                         class="d-flex align-items-center justify-content-between px-2 py-1 border-top"
                         style="flex-shrink:0;">
                        <button class="btn btn-sm btn-outline-secondary py-0 px-2"
                                t-att-disabled="!hasPrevPage" t-on-click="prevPage">
                            <i class="fa fa-chevron-left"/>
                        </button>
                        <small class="text-muted">
                            Pág. <t t-esc="state.page"/> / <t t-esc="totalPages"/>
                            · <t t-esc="state.totalFiltered"/> registros
                        </small>
                        <button class="btn btn-sm btn-outline-secondary py-0 px-2"
                                t-att-disabled="!hasNextPage" t-on-click="nextPage">
                            <i class="fa fa-chevron-right"/>
                        </button>
                    </div>
                </t>

            </t>
        </div>
    </t>

</templates>

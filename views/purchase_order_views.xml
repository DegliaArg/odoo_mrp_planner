<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Botón "Panel" en el form de purchase.order -->
    <record id="purchase_order_form_inherit_planner" model="ir.ui.view">
        <field name="name">purchase.order.form.inherit.planner</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="action_open_planner_dashboard"
                        type="object"
                        class="oe_stat_button"
                        icon="fa-tachometer">
                    <div class="o_stat_info">
                        <span class="o_stat_text">Panel</span>
                    </div>
                </button>
            </xpath>
        </field>
    </record>

</odoo>
